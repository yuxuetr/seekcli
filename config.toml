[brain]
# provider: "openai" (DeepSeek /chat/completions) or "anthropic" (DeepSeek /anthropic)
provider = "openai"
flash_model = "deepseek-v4-flash"
pro_model = "deepseek-v4-pro"

[sensor]
vlm_model = "step-1o-turbo-vision"
