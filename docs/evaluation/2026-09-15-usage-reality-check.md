# 五评：使用现实核查

> 评估时间：2026-09-15
> 被评估方：SeekCLI `main`（阶段四十一完成后）
> 基线：[2026-09-13 四评](2026-09-13-fourth-assessment.md)
> 坐标系：**E 口径（真实使用）**——前四评都在问「建成了什么」，本次问「用上了什么」

---

## 0. 结论

**工程完成度与使用度之间存在数量级落差。** 前四评的所有结论都成立，本次不推翻任何一条。
本次新增的是一个此前四轮都没有量过的维度：

| 口径 | 09-13 四评 | 本次 |
| --- | --- | --- |
| A. 纠偏机制 | 98% | 98%（未复核，无理由变化） |
| B. 组件全景 | 85% | 85%（同上） |
| C. 产品形态 | 75% | 75%（同上） |
| D. 自进化地基 | 约 75% | 约 75%（同上） |
| **E. 真实使用** | — | **约 5%** |

E 口径不是「产品形态」的重复。C 问的是「能不能被别人装上跑起来」（四平台产物、
安装路径、文档），答案是能。E 问的是「作者本人日常是否在用」，答案是否。

---

## 1. 证据

全部取自 `~/.seekcli/`，核查时间 2026-09-15。

### 1.1 审计日志：767 条调用，全部是测试

`~/.seekcli/audit.jsonl`，时间跨度 2026-08-22 ~ 2026-09-13。

| 工具 | 次数 | outcome |
| --- | --- | --- |
| `run_shell` | 320 | 93 executed / 227 denied |
| `read_file` | 162 | 全 denied |
| `write_file` | 106 | 全 denied |
| `harness_inspect` | 78 | 全 denied |
| `no_such_tool` | 73 | 全 denied |
| `propose` | 25 | — |
| `edit_file` | 1 | denied |

**总计 executed 93 / denied 674。** 大量 denied 是 read-only 模式下的**预期**结果，
不是缺陷——它们来自 eval 套件里的拒绝路径断言（`denied_write_then_reports_the_value_instead`、
`blocked_command_reason_reaches_the_model` 等）。`no_such_tool` 73 次同理是
**故意的负例**（`args_bytes: 2`，即 `{}`），不是模型幻觉。

决定性的是 cwd 分布：

| cwd | 次数 | 性质 |
| --- | --- | --- |
| `/Users/hal/cli/seekcli` | 436 | 开发自身 |
| `T/seekcli-loop-readonly` 等四个 | 213 | `loop_tests.rs` 集成测试夹具 |
| `~/.seekcli/bench/*` | 14 | eval testbed |

**没有一条来自真实工作目录。**

### 1.2 会话：75 个，全是冒烟 prompt

最近一次交互式会话创建于 **2026-08-23**。标题样本：

```
2026-08-23  用 mcp__fs__read_text_file 这个工具读取 sample.txt，只告诉我
2026-08-22  用 read_file 工具读取 big.txt，不要用 shell。然后告诉我预览里的第一行。
2026-08-22  我的幸运数字是 4173，记住它。
2026-07-21  先读 z.md，如果不存在就创建，内容是 # Z          （× 7 次）
2026-07-21  先读 a.md 和 b.md 这两个文件，如果不存在就各自创  （× 8 次）
2026-06-17  read config.py then use edit_file
```

九月的 502 条审计记录没有对应的新会话——它们来自 9/13 跑 eval 套件（`bench/` 下
testbed 目录时间戳一致）。

### 1.3 生态装配：全部为零

| 项 | 状态 |
| --- | --- |
| 配置的 MCP server | **0**——`config.toml` 里整段是注释，8/23 后未改动 |
| 安装的 launchd 任务 | **0**——`launchctl list` 无 seekcli 项 |
| `tasks/reminders.md` | 一行 `# Reminders`，空 |
| `tasks/digest/` | 仅 `2026-08-22.md` 一个文件 |
| 累计 proposal | **1** 个，`skill/user_lucky_number`——冒烟测试产物 |

### 1.4 Skill：三个真实场景里两个不存在

| Skill | 行数 | 资产 | 性质 |
| --- | --- | --- | --- |
| `doc_parser` | 79 | `scripts/` × 2 | **唯一为使用而建的** |
| `translator` | 8 | `scripts/` | 壳 |
| `code_reviewer` | 27 | — | 半成品 |
| `ielts_coach` | **6** | — | 提示词壳 |
| `ielts_writing` | **6** | — | 提示词壳 |
| `ielts_speaking` | **6** | — | 提示词壳 |
| 金融 / CQF | — | — | **不存在** |

`ielts_coach` 正文全文是一句「你是一位全能雅思备考教练」。

---

## 2. 一处需要更正四评的表述

四评 §4 写：「五项没有一项是『忘了』——每项都有写明的理由。」这句**事实正确但归因不全**。

`LoopHook` 无第二用户（L1-1）、`SessionStore` 立项理由被推翻（L4-7）、
`allowed_tools` 停在「phase 12.5 will wire」（L5-7）——四评把它们各自归因为独立的取舍。
本次核查给出一个更简单的共同解释：

> **它们缺的不是判断，是真实负载。** 判据「出现第二个实现时翻转」本身没问题，
> 问题是零真实使用的情况下，连第一个实现都不会出现。

这不削弱四评的结论，但改变了下一轮该做什么：**再做第五轮自评是边际收益最低的动作。**

---

## 3. 本次定义的缺口

| # | 缺口 | 证据 | 性质 |
| --- | --- | --- | --- |
| L1-7 | 中断不进子代理：`engine.rs:382` 与 `engine.rs:962` 两处检查均为 `depth == 0` 门控 | 代码核查 | 缺陷级 |
| L4-9 | 事件日志名实不符：`history.rs:8` 自称 append-only，实为 `fs::write` 整文件重写，且整轮末尾才落盘 | 代码核查 | 缺陷级 |
| L4-10 | 压缩阈值与模型窗口脱钩：150K / `KEEP_TAIL=8` 为全局常量，而 provider 由配置静态选择 | 代码核查 | 缺陷级 |
| L4-11 | 无跨会话领域状态：学习进度 / 关注标的这类持续数月的状态无处安放 | §1.4 + 场景 | 功能级 |
| L5-7 | Skill 的 `allowed_tools` 解析后未消费，而 SubAgent 同名字段真裁剪——权限语义不对称 | `skills.rs:286` | **安全级** |
| L2-10 | 17 个内置工具全为本地，无出网能力；金融 / CQF / 问答三场景共同失败原因 | §1.4 + 工具表 | 功能级 |
| L3-6 | 不可信输入无边界：网页 / 文档内容与系统指令无区分 | 代码核查 | **安全级**，随 L2-10 同时出现 |
| L7-9 | trace 与事件日志是两个互不相识的身份空间，无法归因「这次改文件来自哪次调用」 | 代码核查 | 功能级 |
| L7-10 | 30 个 eval 任务全为自造夹具，无真实失败集——改进闭环缺输入 | §1.1 | 结构级 |

L3-6 与 L2-10 是同一次变更的两面：**现在零防护是因为现在根本取不到外部内容**，
一旦 L2-10 补上，L3-6 立刻从「不适用」变成「必须」。

---

## 4. 方法学收获

前四评的方法是**读代码与文档，对照缺口清单打分**。这套方法有一个盲区：
它衡量的全部对象都在仓库里，而仓库里不会记录「作者今天没有用它」。

本次只多做了一件事——**读 `~/.seekcli/` 而不是读 `src/`**。运行时目录是产品与用户
之间唯一的接触面，它记录的是行为而不是意图。审计日志、会话标题、配置文件的
修改时间、proposal 的数量，这四样东西在任何自评里都不会出现，但它们是
E 口径唯一的证据来源。

**建议纳入后续评估的固定动作**：每轮自评先核查运行时目录，再读源码。
如果运行时目录是空的，那一轮评估的其余部分就只是在衡量一个没人用的东西建得有多好。
